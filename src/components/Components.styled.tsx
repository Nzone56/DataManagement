import { Box } from "@mui/material";
import styled from "styled-components";

export const RowJustifyFlex = styled(Box)({
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
});
